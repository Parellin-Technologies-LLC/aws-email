version: 0.1
frontend:
    phases:
        preBuild:
            commands:
                - npm ci
        build:
            commands:
                - node -e "console.log( process.env )"
                - npm run build
    artifacts:
        baseDirectory: dist
        files:
            - '**/*'
    cache:
        paths:
            - node_modules/**/*
